import java.util.concurrent.atomic.AtomicReferenceArray

fun main() {
    val eliminationArray = AtomicReferenceArray<Any?>(2)
//    println(eliminationArray.compareAndSet(0, null, Pair(1, null)));
//    println(eliminationArray.compareAndSet(0, Pair(1, null), 2))
//    print(Any()  Any())
}
